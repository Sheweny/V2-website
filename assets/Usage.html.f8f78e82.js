import{_ as e,c as a}from"./app.b2353902.js";const n={},o=a(`<h1 id="usage" tabindex="-1"><a class="header-anchor" href="#usage" aria-hidden="true">#</a> Usage</h1><h2 id="informations" tabindex="-1"><a class="header-anchor" href="#informations" aria-hidden="true">#</a> Informations</h2><p>The command line interface has several prefixes which are <code>sheweny</code> and <code>shw</code>, so for the following commands, you can use one of the 2 prefixes. The help command is also at your disposal if you are too lazy to come to the guide.</p><div class="language-bash ext-sh line-numbers-mode"><pre class="language-bash"><code>sheweny --help
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h2 id="create-command" tabindex="-1"><a class="header-anchor" href="#create-command" aria-hidden="true">#</a> Create command</h2><p>To create a project, you just need to run the following command.</p><div class="language-bash ext-sh line-numbers-mode"><pre class="language-bash"><code>sheweny create <span class="token punctuation">[</span>project_name<span class="token punctuation">]</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Then you will have a series of questions that will allow you to refine the result of your project creation and to have a bot start that meets your expectations. Depending on what you have answered, you will have access from the start, the creation of the project has commands, events, or interactions.</p><p>For more information, you can run the help command: <code>sheweny --help create</code></p><h2 id="add-command" tabindex="-1"><a class="header-anchor" href="#add-command" aria-hidden="true">#</a> Add command</h2><p>To add a template in your project with the sheweny framework, you just need to run the following command.</p><div class="language-bash ext-sh line-numbers-mode"><pre class="language-bash"><code>sheweny <span class="token function">add</span> <span class="token operator">&lt;</span>add_type<span class="token operator">&gt;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You have several types of template addition depending on what you want to add</p><ul><li>command</li><li>event</li><li>button</li><li>selectmenu</li><li>inhibitor</li></ul><p>After choosing the type of addition, you can answer the questions that are offered to once again refine the result of adding a template.</p><p>For more information, you can run the help command: <code>sheweny --help add</code></p><p>After having done all this, you can of course edit the project code as you wish with an editor, using the rest of the Guide or with the Documentation.</p>`,17);function t(s,i){return o}var r=e(n,[["render",t],["__file","Usage.html.vue"]]);export{r as default};
